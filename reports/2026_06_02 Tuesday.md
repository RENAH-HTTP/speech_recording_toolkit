## Day 1 — Tuesday June 2

- I broke the entire project down into pseudocode before writing any actual code, starting with designing the three-module architecture. 

- I planned out all the functions for metadata_manager.py and what they'd need as inputs and outputs before getting started. 

- For the tech choices, I went with dictionaries for the working database and CSV for the machine learning handoff. PyDub would handle audio quality control, while PyWebView paired with HTML/CSS, created with help from AI, made up the UI.

- Along the way, I picked up some new tricks like using set() for validating allowed values and refreshing my skills on safe dictionary access with .get(). 

- Then I built metadata_manager.py, adding in validation, regex filename checks, CSV input/output, and duplicate detection.

> **Total hours: ~8h (19:00am → 3am)**

| | |
|---|---|
| **Situation** | Wanted to build a speech recording toolkit, started from scratch. |
| **Task** | Design and build the metadata module and UI in one session. |
| **Action** | Pseudocode first, planned functions before coding, built metadata_manager.py + UI shell with AI assist. |
| **Result** | Working module + UI in 8h after a full shift. |